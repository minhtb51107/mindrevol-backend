# --- 1. LOCAL DATABASE CONNECTION ---
spring.datasource.url=jdbc:postgresql://localhost:5432/mindrevol_db
spring.datasource.username=postgres
spring.datasource.password=511007

# --- 2. LOCAL REDIS ---
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.database=0

# --- 3. DEBUGGING ---
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.com.mindrevol=DEBUG
logging.level.org.springframework.security=DEBUG

# --- 4. RELAXED SECURITY ---
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,https://localhost:5173

# --- 5. OVERRIDE CONFIG TỪ FILE .ENV (FIX LỖI) ---

# JWT Secret (Đọc từ biến môi trường)
app.jwt.secret=${APP_JWT_SECRET}

# Email Config
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# --- 8. UPLOAD DIR (Thư mục lưu ảnh) ---
file.upload-dir=uploads

imagekit.upload-url=https://upload.imagekit.io/api/v1/files/upload
imagekit.url-endpoint=https://ik.imagekit.io/iknwsuuwj
imagekit.public-key=public_m/5h79zR+PmaKgB1cccDcVK+CLA=
imagekit.private-key=private_AY3nSUXokdfJ5y2UEZoKBFzKkss=

app.payment.sepay-token=MinhDepTraiVoDich123

# --- SSL / HTTPS CONFIGURATION ---
server.port=8080
server.ssl.enabled=true
server.ssl.key-store-type=PKCS12
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=password
server.ssl.key-alias=mindrevol